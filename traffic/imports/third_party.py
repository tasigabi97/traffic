import cv2, numpy, termcolor

cvtColor = cv2.cvtColor
colored = termcolor.colored
ndarray = numpy.ndarray
VideoCapture = cv2.VideoCapture
imshow = cv2.imshow
waitKey = cv2.waitKey
destroyAllWindows = cv2.destroyAllWindows
